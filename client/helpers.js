// Example of a helpers function, turns out we haven't need this yet. 
// The scope is in any function I think
Helpers = {
	
    skyroom_connect: function(connection, skyroom_slug, join_or_leave) {
		
    }
}